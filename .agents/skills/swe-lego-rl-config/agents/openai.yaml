interface:
  display_name: "SWE-Lego-RL Config"
  short_description: "Generate and validate Harbor RL configs"
  default_prompt: "Use $swe-lego-rl-config to compose a train config from templates and preflight it."
